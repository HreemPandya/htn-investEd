Activate the virtual environment: Create ```python -m venv .venv``` and activate by: ```.venv\Scripts\Activate.ps1```

Run: ```pip install -r requirements.txt``` to install requirements into the vir. env.